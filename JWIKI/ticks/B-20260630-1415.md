# JWIKI Tick B — 2026-06-30 14:15

> Cron tick (turno B, IDs impares). Ventana: 14:15:00 Europe/Paris (UTC+2).
> **Estado**: SPAWN EJECUTADO — agente `aithera-wiki-inv2` trabajando en background.

## Estado al inicio del tick

- **JWIKI-003** (impar previo, tick B 14:00): raw entregado en `material/JWIKI-003-raw.md` (22237 bytes, 63 hechos verificados). Cerró OK hace 15 min.
- **Tick anterior B (14:00)** cerró correctamente. Diferencia 15 min < 30 min. NO es stall. PROCEDER.
- **Tick A 13:45** cerró OK hace 30 min. JWIKI-004 raw listo (24065 bytes, 86 hechos).
- **Siguiente impar pending** (menor ID): **JWIKI-005 — OpenJarvis (Stanford local-first)**.

## Decisión: PROCEDER

- Diferencia vs tick B anterior: 15 min (normal).
- Material crudo del tick previo entregado.
- No hay alerta de stall.

## Hallazgo técnico del spawn — FUNCIONÓ!!

El tick B 14:00 documentó que el spawn a `aithera-wiki-inv2` fallaba con "Invalid spawn config JSON: {" al pasar el payload por cmd.exe. Causa: las comillas dobles del JSON se perdían en el transporte cmd.exe → mavis.cmd.

**Fix aplicado en este tick**:
- Usar path completo a `mavis.cmd`: `C:\Users\Alejandro\.mavis\bin\mavis.cmd` (en lugar de solo `mavis`, que podía no estar en PATH del subprocess).
- Pasar args como LISTA de Python a `subprocess.run(..., shell=False)` — sin shell, sin re-escaping de cmd.exe.

Resultado: **spawn OK** — el daemon devolvió exit 0 con `messageId: 21, status: delivered`. La sesión nueva del agente es `mvs_c3565c1cffec4d9781e4199d924b7b8e` con taskTreeId idéntico.

## Acción ejecutada

1. `task_queue.md`: JWIKI-005 `pending → in_progress`. Notas añadidas con fecha 14:15 y referencia a este tick. Métricas actualizadas.
2. `wiki-map.md`: fila JWIKI-005 cambiada a in_progress.
3. Spawn `aithera-wiki-inv2` OK — sesión `mvs_c3565c1cffec4d9781e4199d924b7b8e` arrancada.
4. Payload de spawn guardado en `ticks/spawn-005.json` (1217 chars prompt).

## Briefing del investigador (despachado)

- **Agente**: `aithera-wiki-inv2` (turno B)
- **Sesión**: `mvs_c3565c1cffec4d9781e4199d924b7b8e`
- **Objetivo**: `JWIKI-005 — OpenJarvis (Stanford local-first)`
- **Path destino final**: `01_LANDSCAPE/openjarvis.md`
- **Output crudo**: `JWIKI/material/JWIKI-005-raw.md`
- **Mínimo**: 45 hechos verificados, cada uno con URL + fecha de acceso.
- **Estructura**: replicar formato de `material/JWIKI-003-raw.md` (secciones por tema, hechos numerados, snippets, tabla comparativa, pendientes).
- **Notas heredadas**: JWIKI-002 ya menciona OpenJarvis en tabla comparativa. Profundizar en los 5 primitivos, routing dinámico, energía como métrica.

## Lecciones aprendidas (para próximos ticks)

1. **El fix de spawn funciona**. Documentar en `KNOWN_PITFALLS` (cuando se cree JWIKI-231+). Etiquetar como "fixed in tick B 14:15".
2. **Backlog inmediato**: aplicar este fix en ticks futuros (A 14:30, B 14:30, etc.).
3. **Para sesiones previas** que documentaron el spawn fallido: ya no es bloqueante. El workaround de Mavis-producir-directamente puede abandonarse en favor del spawn real.
4. **Considerar** crear un script helper `ticks/spawn_agent.py` que centralice el path completo + lista args + retry, para no tener que repetir este snippet cada tick.

## Próximos ticks

- **14:30 turno A**: si JWIKI-005 raw no está listo todavía, ser paciente. Si sí, escribir doc final con `aithera-wiki-escriba` (el escriba B tomará el raw de JWIKI-003 cuando llegue el turno B 14:45).
- **14:30 turno B**: el siguiente impar pending será **JWIKI-007** (Hermes Agent Nous Research). Reusar el fix de spawn.
- **14:45 turno A**: idem, próximo par pending será JWIKI-006.

## Métricas

- Total planificados: 266
- Verified: 2 (JWIKI-001, JWIKI-002)
- In progress (Turno A): 1 (JWIKI-004)
- In progress (Turno B): 2 (JWIKI-003, JWIKI-005)
- Por turno restantes: ~131 cada uno.
- ETA asumiendo 1 doc/15-min por turno: ~33h netas (igual, pero con spawn OK el throughput mejora porque Mavis no tiene que producir raw).

## Operativo

- Sistema JWIKI ha producido 4 raws en ~3 horas desde bootstrap (1 manual + 3 ticks cron).
- Pipeline estable — el único cuello de botella era el spawn del orquestador.
- Con el fix de este tick, el spawn funciona y la producción se acelera.
- Monitorear: el agente `mvs_c3565c1cffec4d9781e4199d924b7b8e` debería entregar raw en ~5-10 min. Cron self-reminder programado.

---

*Mantenido por Mavis (orquestador). Tick B #3 — 2026-06-30 14:15 Europe/Paris. **Spawn FIX exitoso**.*
