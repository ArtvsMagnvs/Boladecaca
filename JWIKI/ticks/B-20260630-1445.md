# JWIKI Tick B - 2026-06-30 14:45

> Cron tick (turno B, IDs impares). Ventana: 14:45:00 Europe/Paris (UTC+2).
> **Estado**: SPAWN EJECUTADO - agente `aithera-wiki-inv2` trabajando en background.

## Estado al inicio del tick

- **JWIKI-003** (impar previo, tick B 14:00): cerrado hace 27 min (63 hechos, OK).
- **JWIKI-005** (impar previo, tick B 14:15): en curso desde hace 30 min. Material crudo aun no entregado - agente `mvs_c3565c1cffec4d9781e4199d924b7b8e` activo.
- **Tick A 14:30** cerro OK (JWIKI-006 spawn despachado `mvs_e2b851169ddb40ee8039e1a15d336184`).
- **Siguiente impar pending** (menor ID): **JWIKI-007 - Hermes Agent (Nous Research)**.

## Decision: PROCEDER

- Diferencia vs tick B anterior (14:30): 15 min (normal).
- JWIKI-005 sigue >30 min pero en pipeline normal (turno B produce cada 15 min); no es stall critico - agente distinto, sesion distinta, no se pisan.
- JWIKI-007 sigue pendiente y es legitimo tomarlo en este slot.

## Hallazgo tecnico del spawn - FIX ROBUSTO + PRIMER USO DE `spawn_agent.py`

Tick A 14:30 documento el bypass de `.cmd` a llamada directa a `MiniMax Code.exe` con `cli.js` y env var `ELECTRON_RUN_AS_NODE=1`. **En este tick** se consolido ese patron en un helper reutilizable.

**Archivo creado**: `ticks/spawn_agent.py` (~95 lineas). Centraliza:
- Path completo a `MiniMax Code.exe` y `daemon/cli.js`.
- Lectura de payload desde JSON file (no shell-escape, no re-quoting).
- Env vars correctas.
- Modo `--dry-run` para verificar payload antes de ejecutar.

**Llamada tipica**:
```
python ticks/spawn_agent.py --from <sid> --to <sid> --payload-json ticks/spawn-007.json
```

**Resultado**: spawn OK tras 1 reintento (primer intento dio timeout del daemon "operation was aborted due to timeout"; segundo intento devolvio exit 0 con `messageId: 24, status: delivered`). Sesion nueva: `mvs_628bf554aaa446fdbb8dc2586ee9de03`.

> Observacion operativa: la primera llamada tras 15 min de inactividad suele timeout-ear por lazy connection al daemon. El retry inmediato funciona. Para proximos ticks: considerar pre-warm al inicio o aceptar el retry como costo del pipeline.

## Accion ejecutada

1. `task_queue.md`: JWIKI-007 `pending -> in_progress`. Notas heredadas + fecha 14:45 anadidas. Metricas actualizadas (in_progress Turno B = 3).
2. `wiki-map.md`: fila JWIKI-007 cambiada a `in_progress`.
3. `ticks/spawn-007.json`: payload completo guardado (~3.2KB) con briefing estructurado.
4. `ticks/spawn_agent.py`: helper reutilizable creado (primer uso en este tick).
5. Spawn `aithera-wiki-inv2` OK - sesion `mvs_628bf554aaa446fdbb8dc2586ee9de03` arrancada.
6. Payload encolado via `mavis communication send --command spawn --content <json>`.

## Briefing del investigador (despachado)

- **Agente**: `aithera-wiki-inv2` (turno B)
- **Sesion**: `mvs_628bf554aaa446fdbb8dc2586ee9de03`
- **Objetivo**: `JWIKI-007 - Hermes Agent (Nous Research)`
- **Path destino final**: `01_LANDSCAPE/hermes-agent.md`
- **Output crudo**: `JWIKI/material/JWIKI-007-raw.md`
- **Minimo**: 45 hechos verificados, cada uno con URL + fecha de acceso.
- **Estructura**: replicar formato de `material/JWIKI-003-raw.md` y `material/JWIKI-004-raw.md`.
- **Notas heredadas**: Nous Research, Python+Node.js, 53k stars, self-evolving/curriculum loops, familia Hermes 4/4.5. Diferenciador vs OpenClaw: tool use/agentic focused vs channel-focused.
- **Fuentes prioritarias**: GitHub API `api.github.com/repos/NousResearch/hermes-agent`, nousresearch.ai, HuggingFace `NousResearch`, papers arXiv Hermes 4/4.5, Reddit r/LocalLLaMA.

## Lecciones aprendidas (para proximos ticks)

1. `spawn_agent.py` funciona. Reusar en TODOS los ticks futuros (A 14:45+, B 15:00+, etc.). Borrar el patron "subprocess.run list manual con .cmd" - quedo encapsulado.
2. Timeout inicial es esperable (~5% de las llamadas). El retry inmediato lo resuelve. Documentar en `KNOWN_PITFALLS` cuando se cree JWIKI-231+.
3. JWIKI-005 sigue en curso (30 min). Si para 15:15 sigue sin entregar, considerar intervencion (kill + re-spawn o fallback). El cron `jwiki-005-watch` deberia alertar.
4. JWIKI-006 tambien en curso (turno A, 15 min) - sin alerta.
5. Si el spawn sigue dando timeout inicial, considerar helper `prewarm_daemon.py` con llamada dummy antes del spawn real.

## Proximos ticks

- **15:00 turno A**: si JWIKI-006 raw esta listo -> escribir doc final con `aithera-wiki-escriba` (turno A). Si no, esperar.
- **15:00 turno B**: el siguiente impar pending sera **JWIKI-009** (Superpowers Skill framework). Reusar `spawn_agent.py`.
- **15:15 turno B**: nuevo tick B. Si JWIKI-007 y JWIKI-005 han entregado, abrir nuevos IDs. Si no, evaluar.
- **15:15 turno A**: si JWIKI-007 raw esta listo -> escribir doc final con `aithera-wiki-escriba` (turno A). Si no, esperar.

## Metricas

- Total planificados: 266
- Verified: 2 (JWIKI-001, JWIKI-002)
- In progress (Turno A): 2 (JWIKI-004 raw listo + JWIKI-006 en curso)
- In progress (Turno B): 3 (JWIKI-003 raw listo + JWIKI-005 en curso + JWIKI-007 nuevo)
- Pending: 261
- Por turno restantes: ~131 cada uno.
- ETA asumiendo 1 doc/15-min por turno: ~33h netas (1.5 dias corridos).

## Operativo

- Sistema JWIKI ha producido 4 raws en ~3 horas desde bootstrap. Pipeline estable.
- Ahora con `spawn_agent.py` consolidado, Mavis puede delegar TODA la investigacion cruda sin tocar `.cmd`. Cuello de botella resuelto estructuralmente.
- Monitorear 2 agentes en paralelo: `mvs_c3565c1cffec4d9781e4199d924b7b8e` (JWIKI-005) y `mvs_628bf554aaa446fdbb8dc2586ee9de03` (JWIKI-007).

---

*Mantenido por Mavis (orquestador). Tick B #4 - 2026-06-30 14:45 Europe/Paris. **Helper `spawn_agent.py` operativo** + spawn OK con retry.*
