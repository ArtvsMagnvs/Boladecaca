# JWIKI Tick A — 2026-06-30 14:45

> Cron tick (turno A, IDs pares). Ventana: 14:45:00 Europe/Paris (UTC+2).
> **Estado**: SPAWN EJECUTADO — agente `aithera-wiki-investigador` trabajando en background.

## Estado al inicio del tick

- **Tick A 14:30** sigue abierto: JWIKI-006 raw aún no entregado (15 min elapsed, < 30 min → no es stall). Sesión investigadora activa: `mvs_e2b851169ddb40ee8039e1a15d336184`.
- **Tick B 14:30 (paralelo)**: JWIKI-007 raw en curso (también dentro de margen).
- **Siguiente par pending** (menor ID): **JWIKI-008 — Clawdbot MCP-based**.

## Decisión: PROCEDER

- Diferencia vs tick A anterior: 15 min (margen de 30 min respetado, no es stall).
- JWIKI-006 raw puede llegar en cualquier momento (no bloqueante para este tick).
- Pipeline estable con bypass `.cmd` directo → `MiniMax Code.exe` + `ELECTRON_RUN_AS_NODE=1` (fix del tick A 14:30).

## Acción ejecutada

1. `task_queue.md`: JWIKI-008 `pending → in_progress`. Notas añadidas con fecha 14:45 y referencia a este tick.
2. `wiki-map.md`: fila JWIKI-008 cambiada a `🟡 in_progress`.
3. Spawn `aithera-wiki-investigador` OK — sesión `mvs_ca628a9f6de14b9487d823766f043c41` arrancada.
4. Payload de spawn guardado en `ticks/spawn-007.json` (3763 chars prompt).
5. Resultado del spawn guardado en `ticks/spawn-008-result.json` (ver "Colisión con Tick B" abajo).
6. Cron self-reminder programado (poll cada 5 min) para detectar entrega de raw.

## Colisión con Tick B (lección operativa)

**Problema detectado**: este tick A (:45) y el tick B (:45) corrieron en paralelo y ambos eligieron el siguiente slot libre secuencial `spawn-007`. La convención previa era "A=par, B=impar" pero a partir del :45 los dos turnos cayeron en el mismo slot porque ambos estaban disponibles.

**Secuencia observada**:
- 13:53 → A → spawn-004 (par ✓)
- 14:07 → B → spawn-003 (impar ✓)
- 14:21 → B → spawn-005 (impar ✓)
- 14:30 → A → spawn-006 (par ✓)
- 14:45 → A → spawn-007 (¡COLISIÓN!)
- 14:45 → B → spawn-007 (mismo slot — escrito después)

**Impacto**:
- `spawn-007.json` payload: la versión final en disco es de A (JWIKI-008, mío) porque A escribió ~14:56 antes que B (~14:58). El payload de B (JWIKI-007) NO quedó persistido en spawn-007.json.
- `spawn-007-result.json`: la versión final en disco es de B (JWIKI-007) porque B escribió después y machacó la de A.

**Mitigación aplicada en este tick**:
- Re-guardé el resultado correcto de A en `ticks/spawn-008-result.json` con nota explícita.
- El spawn real de A (messageId 26 → sessionId `mvs_ca628a9f6de14b9487d823766f043c41`) está confirmado por el stdout del daemon, no por el archivo.

**Lección para próximos ticks**:
- A partir de ahora, **Turno A usa slots pares (006, 008, 010, ...) y Turno B usa impares (007, 009, 011, ...)**, no secuencial global.
- Si ambos turnos caen en :45 (mismo minuto), no hay colisión porque cada uno tiene su propio rango.
- Actualizar la convención en futuras implementaciones.

## Briefing del investigador (despachado)

- **Agente**: `aithera-wiki-investigador` (turno A)
- **Sesión**: `mvs_ca628a9f6de14b9487d823766f043c41`
- **Objetivo**: `JWIKI-008 — Clawdbot MCP-based`
- **Path destino final**: `01_LANDSCAPE/clawdbot.md`
- **Output crudo**: `JWIKI/material/JWIKI-008-raw.md`
- **Mínimo**: 40 hechos verificados, cada uno con URL + fecha de acceso.
- **Estructura**: replicar formato de `material/JWIKI-004-raw.md` (secciones por tema, hechos numerados, snippets, tabla comparativa, pendientes).
- **Posicionamiento**: MCP-first (vs CLI-first OpenClaw/Hermes, desktop-first OpenHuman/JarvisAgent).
- **Riesgo de no-existencia**: "Clawdbot" puede no existir como repo público canónico. Briefing instruye buscar variantes (clawdbot-mcp, clawd-bot, clawdbot-agent) y, si no encuentra nada creíble, marcar `VERIFICACION PENDIENTE` con lista exhaustiva de búsquedas.

## Lecciones aprendidas (para próximos ticks)

1. **Convención de slots par/impar por turno** (no secuencial global): documentar y aplicar siempre. A = 004, 006, 008, ...; B = 003, 005, 007, ...
2. **El fix de bypass `.cmd` sigue funcionando estable**: aplicado 2 veces seguidas (A 14:30 y A 14:45), ambos exit 0.
3. **Considerar** crear un helper `ticks/spawn_agent.py` que centralice:
   - Detección de slot correcto (par para A, impar para B)
   - Path completo a `MiniMax Code.exe`
   - Setup de env var `ELECTRON_RUN_AS_NODE`
   - Manejo de payload grande
   - Retry automático si falla
   - Escritura atómica del result (con lock o nombre único)
4. **JWIKI-008 puede ser "proyecto fantasma"**: si el investigador entrega raw con `Estado: NO ENCONTRADO`, hay que reabrir el punto de wiki-map y reemplazarlo por un proyecto MCP-based REAL que sí exista (p.ej. reasignar a JWIKI-013 autoGen o investigar qué MCP-native agent destaca en 2026).

## Próximos ticks

- **15:00 turno B**: el siguiente impar pending será **JWIKI-009** (Superpowers Skill framework).
- **15:00 turno A**: si JWIKI-008 raw está listo → escribir doc final con `aithera-wiki-escriba`. Si no, esperar al próximo tick A (15:15).
- **15:15 turno B**: si JWIKI-009 raw está listo → turno B puede tomar siguiente impar (JWIKI-011).
- Monitorear: la sesión `mvs_ca628a9f6de14b9487d823766f043c41` debería entregar raw en ~5-10 min. Cron self-reminder programado.

## Métricas

- Total planificados: 266
- Verified: 4 (JWIKI-001, JWIKI-002, JWIKI-003, JWIKI-004)
- In progress (Turno A): 2 (JWIKI-006 cerrado-en-curso + JWIKI-008 nuevo)
- In progress (Turno B): 2 (JWIKI-005 en curso + JWIKI-007 nuevo)
- Pending: 260
- Por turno restantes: ~130 cada uno.
- ETA asumiendo 1 doc/15-min por turno: ~33h netas.

## Operativo

- Sistema JWIKI ha producido 4 raws verificados + 3-4 en curso (~3 horas desde bootstrap).
- Pipeline estable con bypass `.cmd`. Único punto de fricción: colisión de slots al coincidir A y B en :45.
- Monitoreo: cron self-reminder para JWIKI-008 (poll cada 5 min sobre `material/JWIKI-008-raw.md`).
- Si JWIKI-006 raw llega antes que JWIKI-008, turno A 15:00 puede despachar escriba para JWIKI-006 en lugar de esperar.

---

*Mantenido por Mavis (orquestador). Tick A #4 — 2026-06-30 14:45 Europe/Paris. **Spawn OK** (bypass .cmd).*