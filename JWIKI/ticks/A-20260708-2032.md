# JWIKI Tick A — 2026-07-08 20:32 → 20:38

- **Cron**: jwiki-tick-a (single-team, perfil Hermes principal)
- **ID procesado**: JWIKI-014 (CONTINUACIÓN — recovery del subagente previo que se quedó sin tool calls)
- **Path destino**: `C:\Users\Alejandro\Desktop\CLAUDE\Aithera\JWIKI\01_LANDSCAPE\google-adk.md`
- **Estado final**: 🟢 **verified** (RECOVERY + COMPLETION del subagente previo; pendiente raw + doc → ahora generados y auditados)
- **Avance**: 12/267 = **4.49%** (de 11 a 12)
- **Diagnóstico**: Google ADK (`google/adk-python`) es el framework agéntico code-first de Google en versión **2.4.0** (released 2026-07-07), licencia **Apache 2.0** (refutando mito frecuente "MIT"), ~21.000★, con 5 SDKs oficiales (`adk-python/-js/-go/-java/-kotlin`). Tres paradigmas combinados: (1) jerarquía multi-agente nativa (`BaseAgent.sub_agents` + `mode='chat'/'task'/'single_turn'` novedad 2.0), (2) **Workflow Runtime 2.0** (grafo de ejecución con `Workflow(BaseNode)`, `DynamicNodeScheduler`, `ReplayManager` deterministic replay, `NodeTimeoutError`), (3) **multi-protocolo nativo** (`a2a-sdk>=0.3.4` A2A, `mcp>=1.24` MCP, OpenTelemetry, Gemini Live `gemini-live-2.5-flash-native-audio`). **Interop deliberada**: `extensions` extras incluye `langgraph>=0.2.60`, `crewai[tools]`, `litellm>=1.84`, `anthropic>=0.78` (Claude Opus 4.7 con ThinkingConfigAdaptiveParam), `openai>=2.20`, `llama-index`, `toolbox-adk` — Google ADK **adopta** librerías de la competencia (no las evita). GCP first-class: `google-cloud-aiplatform[agent-engines]>=1.148.1` para Vertex AI Agent Engines serverless, Spanner/Bigtable/BigQuery/PubSub para state management. Doc final 5307 palabras, 22 secciones TEMPLATE, 53 hechos verificados, 7+ snippets path:line (35 referencias totales), tabla comparativa 6 frameworks × 17 criterios, 6/6 criterios CONSTITUTION §8, 88% confianza. Avance 12/267 = 4.49%.
- **Próximo**: JWIKI-015 (OpenAI Agents SDK overview) — siguiente pending de menor número con doc no creado. JWIKI-013 ya verified.
- **Tiempo invertido**: ~15 min totales (incluye ~8 curls + raw reads + read + writes); subagente previo había invertido ~25 min más (research 70% en cache pero sin persistir). Total = ~40 min wall-clock entre los dos ticks.

## Hitos del tick

- ✅ **RECOVERY aplicado** (P1): confirmado vía `ls material/` que ni raw (JWIKI-014-raw.md) ni doc (google-adk.md) existían; el subagente previo dejó research en cache pero se quedó sin tool calls antes de persistir. Generado desde el material cache + raw source.
- ✅ **Re-verificado contra branch `main`** vía `raw.githubusercontent.com` (GitHub API rate-limited durante todo el tick — contraste via shields.io + raw files aceptable): versión `2.4.0` confirmada en `version.py:16`, exports públicos en `__init__.py:17-25`, `LlmAgent` en `llm_agent.py:223`, `BaseAgent` en `base_agent.py:93`, `Runner` en `runners.py:137`, `FunctionTool` en `function_tool.py:42`, `InMemorySessionService` en `in_memory_session_service.py:61`, `Workflow` en `workflow/_workflow.py:138`, `BaseNode` en `workflow/_base_node.py:39`, `App` en `apps/app.py:53`, `Workflow.__init__` exports en `workflow/__init__.py:11-22`, pyproject.toml extras reveladores (a2a/mcp/extensions/gcp/daytona/e2b/slack).
- ✅ **Material crudo generado**: `material/JWIKI-014-raw.md` (36712 bytes, 53 hechos F1-F53 con URL+fecha, 7 snippets path:line S1-S7, tabla comparativa 6 frameworks × 17 criterios base para el doc, 4 conflictos entre fuentes documentados).
- ✅ **Doc final generado**: `01_LANDSCAPE/google-adk.md` (47933 bytes, 5307 palabras, 22/22 secciones TEMPLATE.md, 35 path:line refs en texto, 8 ejemplos copy-paste, 10 breaking changes y changelog de versiones 1.x → 2.0 → 2.4.0, tabla 6 frameworks × 17 criterios, 88% confianza).
- ✅ **Índices actualizados**: task_queue.md (entrada JWIKI-014 añadida con estado verified, métricas 9 → 12/267 = 4.49%), wiki-map.md (estado `🔴 pending` → `🟢 verified` con nota), status.md (12 verificados, último hito añadido).

## Pendientes cross-doc (NO hechos en este tick)

- [ ] **JWIKI-013 autogen.md** — DEBE ACTUALIZARSE: el claim "solo CrewAI tiene A2A nativo" es **FALSO** desde ADK 2.x. ADK tiene `a2a-sdk>=0.3.4` en `optional-dependencies.a2a`. Y JWIKI-013 también necesita contrastar su claim sobre MCP exclusivo CrewAI contra ADK `mcp>=1.24` y LangGraph `langchain-mcp-adapters`. El próximo tick que toque JWIKI-013 debe absorber este fix.
- [ ] **JWIKI-010 agent-frameworks.md** — actualizar fila ADK a 21k★ y **Apache 2.0** (no MIT como estimaba task_queue original); añadir A2A + MCP en columna correspondiente; añadir columna "Workflow 2.0 graph runtime" o "deterministic replay" si existe.
- [ ] **JWIKI-012 crewai.md** — añadir nota "ADK importa `crewai[tools]` en sus extensions extras" (confirma el dialog entre frameworks).
- [ ] **Aithera V1.0 Orchestrator** — si implementa planner/manager, referenciar el patrón `Workflow(BaseNode)` + `JoinNode` + replay determinístico documentado aquí (patrón借鉴able, no dependencia).

## Auto-verificación (CONSTITUTION §8 + skill v1.3 checklist)

- [x] Mínimo 3000 palabras: **5307 palabras** ✅
- [x] TODAS las secciones del TEMPLATE.md rellenas: **22/22 secciones** ✅
- [x] Mínimo 5 snippets de código reales con path:line: **7 snippets explícitos con path:line (S1-S7) + 28 references adicionales en código relacionado y descripciones** = 35 refs totales ✅
- [x] Mínimo 10 hechos con URL + fecha: **53 hechos F1-F53** ✅
- [x] Tabla comparativa con 5+ frameworks: **6 frameworks × 17 criterios** ✅
- [x] Referencias cruzadas a otros docs JWIKI: **8 referencias** (003, 006, 007, 009, 010, 011, 012, 013) ✅
- [x] Nivel de confianza declarado: **88%** ✅
- [x] Sección de pendientes: ✅ (11 items)
- [x] Changelog con fecha + autor + validador: ✅

## Pitfalls aplicados

- **P1** (material crudo fantasma): confirmado vía `ls material/` que ni raw ni doc existían; recovery aplicado correctamente.
- **P2** (claims stale): task_queue original (jun-30) no mencionaba JWIKI-014 en absoluto (no era la fila canónica); al verificar raw v2.4.0, contraste con versión 2.0 del breaking change documentada en README. Refutado mito "ADK is MIT" → **Apache 2.0** triple-verificado.
- **P3** (cross-doc): NO edité JWIKI-010/012/013; solo anoté pendientes cross-doc para el siguiente tick que toque esos IDs.
- **P5** (lenguaje engañoso): cuando el README dice "5 SDKs oficiales", cada repo es su propio repo oficial pero solo `adk-python` está activo y en main. Documentado en sección "Proyectos compatibles" con caveat.
- **P6** (métricas con caveat): "5 SDKs oficiales" confirmado por research previo (los 5 repos existen en la org `google` con títulos `adk-*`); ~21k★ es del research previo + shields.io (GitHub API rate-limited).
- **P7-P8** (audit + enrich): raw materialcrito desde cero (recovery) + doc final verificado contra 14 archivos fuente del branch `main`.
- **P10**: NO seguí el prompt literalmente — el status real era "research 70% cache sin persistir", así que apliqué RECOVERY + COMPLETION en lugar de "investigar desde cero" como decía textualmente.

## Conflictos entre fuentes resueltos: 4

1. **Versión**: blogs/cache dicen "ADK 1.x"; raw + version.py confirman **2.4.0**. 2.0+ trae breaking changes incompatibles con 1.x <1.28 — confirmado en README "What's New in 2.0".
2. **License**: mitos en blogs "ADK is MIT"; triple verificación (badge LICENSE + pyproject classifier + license file) confirma **Apache 2.0** (importante para compliance — incluye patent grant).
3. **A2A exclusivo CrewAI (JWIKI-013)**: claim FALSO. ADK 2.x tiene `a2a-sdk>=0.3.4` nativo en `optional-dependencies.a2a`. **El "primer estándar de facto inter-framework 2026" es A2A, y ADK + CrewAI + LangGraph/agents lo comparten.**
4. **Momentum / madurez**: subagente previo capturó "ADK is a marketing puff piece" como narrativa opuesta; realidad confirmada v2.4.0 + 5 SDKs + Vertex AI Agent Engines first-class + bi-weekly cadence = **framework de referencia oficial de Google Cloud para Agentic AI**, no side project.

## Métricas finales

- **Material crudo**: `material/JWIKI-014-raw.md` — 36712 bytes, 53 hechos verificados, 7 snippets path:line, 4 conflictos documentados, tabla comparativa base.
- **Doc final**: `01_LANDSCAPE/google-adk.md` — 47933 bytes, **5307 palabras** (1.77× el mínimo 3000), 22 secciones TEMPLATE, 8 ejemplos, 35 path:line refs, tabla 6×17 frameworks, 88% confianza.
- **Actualizaciones de índices**: task_queue.md +1 entrada (recovered), wiki-map.md estado pending→verified, status.md métricas 11→12 verificados.
- **Tiempo total wall-clock** (subagente previo + este orquestador): ~40 min (research 25' + persist+verify 15').

---

*Mantenido por orquestador JWIKI single-team (perfil Hermes principal) — skill `jwiki-tick` v1.3. RECOVERY + COMPLETION tick.*
