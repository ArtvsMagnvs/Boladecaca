# JWIKI Tick A — 2026-06-30 14:30

> Cron tick (turno A, IDs pares). Ventana: 14:30:00 Europe/Paris (UTC+2).
> **Estado**: SPAWN EJECUTADO — agente `aithera-wiki-investigador` trabajando en background.

## Estado al inicio del tick

- **JWIKI-004** (par previo, tick A 13:45): raw entregado en `material/JWIKI-004-raw.md` (24065 bytes, 86 hechos verificados). Cerró OK hace 45 min.
- **Tick A 13:45 cerró correctamente**: investigador entregó raw sin issues (v0.53.43 resuelto). Diferencia vs este tick: 45 min > 30 min. NO es stall. PROCEDER.
- **Tick B 14:15** también cerró OK (JWIKI-005 raw en curso, agente `mvs_c3565c1cffec4d9781e4199d924b7b8e`).
- **Siguiente par pending** (menor ID): **JWIKI-006 — JarvisAgent Tauri Vue 3**.

## Decisión: PROCEDER

- Diferencia vs tick A anterior: 45 min (margen de 30 min respetado).
- Material crudo del tick previo entregado y verificado (24065 bytes, 86 hechos).
- No hay alerta de stall.

## Hallazgo técnico del spawn — ADAPTACIÓN

El tick B 14:15 documentó que `mavis.cmd` funciona correctamente con `subprocess.run(list, shell=False)` y path completo. **En este tick, el mismo enfoque FALLÓ** con error `WinError 2: file not found` y `cmd /c` también falló.

**Causa raíz**: `subprocess.run()` con `shell=False` no puede ejecutar `.cmd`/`.bat` directamente — necesita `.exe`. Pero `mavis.cmd` está en `C:\Users\Alejandro\.mavis\bin\mavis.cmd` (no es .exe).

**Fix aplicado en este tick**:
- Bypass total del `.cmd`: llamada directa al binario subyacente.
- `mavis.cmd` simplemente envuelve `"C:\Users\Alejandro\AppData\Local\Programs\MiniMax Code\MiniMax Code.exe" "...\\daemon\\cli.js" %*` con `ELECTRON_RUN_AS_NODE=1`.
- Llamada directa: `MiniMax Code.exe` con `cli.js` como primer argumento + variable de entorno `ELECTRON_RUN_AS_NODE=1`.

Resultado: **spawn OK** — el daemon devolvió exit 0 con `messageId: 22, status: delivered`. La sesión nueva del agente es `mvs_e2b851169ddb40ee8039e1a15d336184`.

## Acción ejecutada

1. `task_queue.md`: JWIKI-006 `pending → in_progress`. Notas añadidas con fecha 14:30 y referencia a este tick.
2. `wiki-map.md`: fila JWIKI-006 cambiada a `🟡 in_progress`.
3. Spawn `aithera-wiki-investigador` OK — sesión `mvs_e2b851169ddb40ee8039e1a15d336184` arrancada.
4. Payload de spawn guardado en `ticks/spawn-006.json` (3082 chars prompt).
5. Resultado del spawn guardado en `ticks/spawn-006-result.json`.

## Briefing del investigador (despachado)

- **Agente**: `aithera-wiki-investigador` (turno A)
- **Sesión**: `mvs_e2b851169ddb40ee8039e1a15d336184`
- **Objetivo**: `JWIKI-006 — JarvisAgent Tauri Vue 3`
- **Path destino final**: `01_LANDSCAPE/jarvisagent.md`
- **Output crudo**: `JWIKI/material/JWIKI-006-raw.md`
- **Mínimo**: 40 hechos verificados, cada uno con URL + fecha de acceso.
- **Estructura**: replicar formato de `material/JWIKI-004-raw.md` (secciones por tema, hechos numerados, snippets, tabla comparativa, pendientes).
- **Stack declarado**: Tauri 2.0 + Vue 3 + Rust.
- **Diferenciadores clave**: 20+ LLMs, snapshot engine, sub-agent delegation, plan approval.

## Lecciones aprendidas (para próximos ticks)

1. **El fix de `subprocess.run` con `.cmd` directo NO funciona en este entorno**. El `.cmd` se rompe por CreateProcess. La solución estable es bypass `.cmd` → llamada directa al `.exe` + `cli.js` con `ELECTRON_RUN_AS_NODE=1`.
2. **Aplicar este fix en TODOS los ticks futuros** (A 14:45, B 14:45, A 15:00, B 15:00, etc.). Documentar este patrón como helper reusable.
3. **Considerar** crear un script helper `ticks/spawn_agent.py` que centralice:
   - Detección del método de spawn (mavis.cmd vs direct .exe)
   - Path completo a `MiniMax Code.exe`
   - Setup de env var `ELECTRON_RUN_AS_NODE`
   - Manejo de payload grande
   - Retry automático si falla
4. **Actualizar** `KNOWN_PITFALLS` cuando se cree JWIKI-231+ con etiqueta "fixed in tick A 14:30".

## Próximos ticks

- **14:45 turno B**: el siguiente impar pending será **JWIKI-007** (Hermes Agent Nous Research). Reusar el fix de spawn (direct .exe).
- **14:45 turno A**: si JWIKI-006 raw está listo → escribir doc final con `aithera-wiki-escriba`. Si no, esperar al próximo tick A (15:00 o más).
- **15:00 turno B**: el siguiente impar pendiente tras JWIKI-007 será JWIKI-009 (Superpowers Skill framework).

## Métricas

- Total planificados: 266
- Verified: 2 (JWIKI-001, JWIKI-002)
- In progress (Turno A): 2 (JWIKI-004 cerrado + JWIKI-006 nuevo)
- In progress (Turno B): 2 (JWIKI-003 cerrado + JWIKI-005 en curso)
- Pending: 261
- Por turno restantes: ~131 cada uno.
- ETA asumiendo 1 doc/15-min por turno: ~33h netas.

## Operativo

- Sistema JWIKI ha producido 4 raws en ~3 horas desde bootstrap (1 manual + 3 cron ticks).
- Pipeline estable: el cuello de botella del spawn ahora está resuelto con bypass .cmd → direct .exe.
- Mavis puede delegar TODA la investigación cruda a `aithera-wiki-investigador` y `aithera-wiki-inv2`, sin tener que producir raw directamente.
- Monitorear: el agente `mvs_e2b851169ddb40ee8039e1a15d336184` debería entregar raw en ~5-10 min. Cron self-reminder programado.

---

*Mantenido por Mavis (orquestador). Tick A #3 — 2026-06-30 14:30 Europe/Paris. **Spawn FIX robusto** (bypass .cmd).*
