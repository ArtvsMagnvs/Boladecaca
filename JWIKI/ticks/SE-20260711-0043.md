# Skill Maintenance Tick SE — 2026-07-11 00:43

- Cron: skill-evolve (every 3 days, scheduled at 00:43)
- Target: Aithera-related skills under `~/AppData/Local/hermes/skills/`
- Skills reviewed: 6 (aithera-hub-corestate, aithera-voice-stt, aithera-voice-tts-autoplay, aithera-requirements-discipline, project-handover-onboarding, project-skill-tree-design)
- Skills patched: 3 (aithera-hub-corestate, aithera-voice-stt, aithera-voice-tts-autoplay)
- Skills unchanged: 3 (aithera-requirements-discipline still accurate per latest venv state, project-handover-onboarding is generic and accurate, project-skill-tree-design is generic and accurate)
- jwiki-tick skill NOT touched: P31-P39 already comprehensive; no new pitfalls accumulated in last 3 days

## Diagnosis (what was found)

The 3 Aithera voice/UI skills were authored in V0.7.0 timeframe (2026-07-02) and **had not been updated since V0.8 shipped (2026-07-09)**. Reality check on 2026-07-11 found:

1. **`aithera-hub-corestate` — stale enum count and wrong hard rule §16.3**.
   - Skill said: "6 states, adding a state requires updating 6+ files."
   - Reality (verified in `useAppStore.ts:9` and 4 lookup tables in `frontend/src/components/hub/`): 8 states (`idle | listening | thinking | speaking | processing | error | action | recovering`), and adding a state requires updating exactly 5 files (1 type + 4 lookup tables). The original 6+ claim predates the AVCS S1 refactor.

2. **`aithera-voice-stt` — presented V0.83 as a future goal**.
   - Skill said: "V0.83 goal is real-time voice interaction... what's missing is STT."
   - Reality: V0.83 shipped (2026-07). `MicButton.tsx` (309 lines) exists, is wired into `Chat.tsx:4`, and is used by both single-shot dictation and Modo Conversación.

3. **`aithera-voice-tts-autoplay` — presented autoplay as a future goal; missed B21 reasoning filter and Modo Conversación**.
   - Skill said: "What's missing is autoplay... should add a toggle..."
   - Reality: autoplay is shipped (Chat.speak calls `/api/voice/synthesize` after every reply); TTS is now multi-provider (EdgeTTS + ElevenLabs + Kokoro + eSpeak); Modo Conversación is a continuous voice loop in Chat.tsx:230-280; B21 reasoning filter (V0.8) must NOT speak the `<think>` chain (already handled server-side, but worth documenting as an integration point).

## What I changed (concrete)

### Patch 1 — `aithera-hub-corestate`

- **Overview** rewritten: 6 states → 8 states, hard rule §16.3 corrected from "6+ files" to "exactly 5 files" with explicit list (useAppStore.ts union + AICore.tsx STATE_CONFIG + PoopSphere.tsx AGITATION + RasenganSphere.tsx STATE_SPIN + CoreSelector.tsx STATE_RING_COLOR + STATE_GLOW). Added reasoning for why the original rule was wrong (AVCS S1 refactor predated it).
- **The 6 States (Contract)** section renamed to **The 8 States (Contract — V0.8, 2026-07-09)** with new state table including `action` and `recovering` rows, visual specs from the actual lookup tables, and a **Wiring status** column showing 6/8 are wired today and 2/8 are placeholders for MVP1.
- **Exact Wiring Points** table updated to V0.8 reality: Chat.tsx:130 (sendMessage), Chat.tsx:247+253 (V0.83 Modo Conversación), Chat.tsx:70 (speak), MicButton.tsx:54+ (recording), VoiceCenter.tsx:283, Agents.tsx:206+247, EmailAssistant.tsx (triage), Hub.tsx:73 (consumer only). Plus a "File locations to update when adding a new state" mini-section.
- **Not changed** (still accurate): Producer-Cleanup Pattern, Double-Fire Guard, Verification Checklist (with one note about `coreStateLabel` falling back to raw string for action/recovering).

### Patch 2 — `aithera-voice-stt`

- **Overview** rewritten: title now says "V0.83 SHIPPED", status section added (✅ STT shipped, ✅ TTS multi-provider, ✅ Modo Conversación shipped, ⏳ wake-word out of scope). Cross-reference to `aithera-voice-tts-autoplay` for the TTS half (this skill now only covers STT).
- **Not changed** (still accurate): Architecture diagram, backend faster-whisper singleton pattern, MediaRecorder gotchas, Common Pitfalls, Verification Checklist, One-Shot Recipe, Reference (just updated the verification date stamp).

### Patch 3 — `aithera-voice-tts-autoplay`

- **Overview** rewritten: title now says "V0.83 SHIPPED (multi-provider)", provider table added (EdgeTTS / ElevenLabs / Kokoro / eSpeak with files, setup, default voices, when-to-use).
- **NEW section "B21 — Reasoning filter interaction (V0.8)"** inserted before "Common Pitfalls". Documents that B21 (reasoning_filter.py) strips `<think>` server-side in chat.py, so TTS autoplay already speaks clean text; documents the integration point comment to add if B21 ever moves to the client.
- **NEW section "Modo Conversación (V0.83 — continuous voice loop)"** inserted before "Common Pitfalls". Documents the Chat.tsx:230-280 loop pattern, the per-stage coreState transitions, the stop-signal-via-ref pattern, the no-TTS-queue semantics, and the V0.83 critical fix about side-effects-in-state-updater causing StrictMode double-fire.
- **Not changed** (still accurate): Configuration & persistence, Implementation hook pattern, audioLevel modulation sketch, Common Pitfalls (ElevenLabs 502 propagation still valid — confirmed in repo commit 81650ce).

## What I did NOT change (and why)

- **aithera-requirements-discipline** — re-read in full. The "broken venv" story it captures (commit `849a059` reverting the `faster-whisper` pin, `81650ce` propagating ElevenLabs errors) is still the canonical reference. The recipe cards (`pip install --no-deps`, `--dry-run` pre-commit gate) still apply. No path changes (CLAUDE.md §15 still uses the same `cd backend` + `pip install -r requirements.txt` flow). **No patch needed**.
- **project-handover-onboarding** — generic skill, not Aithera-specific. No reason to touch.
- **project-skill-tree-design** — generic skill about umbrella-vs-flat shape. No reason to touch.
- **jwiki-tick** — re-read in full. P31-P39 (loop auton + RECOVERY+COMPLETION + P39 AI providers Tier-1 source) are comprehensive. The skill's last version (v2.1, 2026-07-09) is current with JWIKI-020 RECOVERY case. No new pitfalls discovered in the 2-day window since last update. **No patch needed**.

## Cross-doc correction handoff (per jwiki-tick skill convention)

When future JWIKI ticks process docs that reference these skills, they should reflect:

- **JWIKI-003 (CLAUDE.md snapshot, future)**: should mention `coreState` has 8 states (not 6) when the doc covers the Hub.
- **JWIKI-005 (Hub 3D AICore.tsx, future)**: should mention the AVCS S1 refactor that added `action` and `recovering` placeholders.

These are NOT urgent — the JWIKI docs for these areas don't exist yet (none of JWIKI-001..020 cover the Aithera frontend in detail). When they do, they should consult the patched skills as the source of truth.

## Self-reminder / next run

This cron runs every 3 days. Next run: 2026-07-14. Likely candidates for next patches (provisional):
- **aithera-requirements-discipline**: check if the `requirements.txt` has changed in the last 3 days. If new pins, verify the skill's pre-commit gate recipe still applies.
- **aithera-hub-corestate**: if MVP1 ships (wiring of `action`/`recovering` producers from doc 13 §4), update the Wiring status column from "⏸ placeholder" to "✅ wired in X.tsx".
- **aithera-voice-tts-autoplay**: if Wake-word detection is added in V0.85, add a wake-word section.

End of tick SE-20260711-0043.