# JWIKI Tick A — 2026-07-08 20:40

- **Cron**: jwiki-tick-a
- **ID procesado**: JWIKI-015
- **Path destino**: `C:\Users\Alejandro\Desktop\CLAUDE\Aithera\JWIKI\01_LANDSCAPE\openai-agents-sdk.md`
- **Estado final**: verified
- **Avance**: 13/267 = 4.87%

## Diagnóstico

Generación desde cero (P1) sobre el SDK oficial de OpenAI para agentes Python. GitHub API rate-limited durante toda la sesión (espera 60s no levantó; segundo intento también rate-limited); contraste via **shields.io JSON endpoint** (`{"value":"28k"}`) + **raw.githubusercontent.com** triple-check (LICENSE + pyproject.toml + src modules) + **docs oficiales MkDocs Material** (`openai.github.io/openai-agents-python/`). Sin git/trees ni /languages ni contributors por rate-limit, pero los raw sources son tier-1 igualmente autoritativos.

Hallazgos clave:
- **v0.18.0** publicada 2026-07-08 (PyPI + shields + pyproject.toml `[project] version`).
- **MIT** (Copyright 2025 OpenAI en LICENSE).
- **28k stars** (shields.io 2026-07-08 18:46 UTC; brief del parent ~27.7k → +1.1% stale).
- **Python >=3.10** (clasificadores 3.10/3.11/3.12/3.13/3.14 — confirma Python 3.14 wheel disponible).
- **Monorepo UV workspace** con `members=["agents"]` y `[tool.hatch.build.targets.wheel] packages=["src/agents"]` (P20 confirmado: layout real es `src/agents/`, NO `agents/agents/`).
- **Default model `gpt-5.4-mini`** con `reasoning.effort="none"` + `verbosity="low"` (verificado en docs/models/index.md, no solo en docstring agent.py:266 — conflicto menor con ejemplo "Haiku agent" que usa `gpt-5-nano`).
- **9 conceptos core** (Agents, Sandbox Agents [beta 0.14.0], Tools, Handoffs, Guardrails, Human-in-the-loop, Sessions, Tracing, Realtime Agents).
- **MCP como dep core** (`mcp>=1.19.0`) — confirmando que el SDK tiene MCP first-class sin extras.
- **NO A2A nativo** — refuta suposición de JWIKI-013 sobre "A2A como protocolo transversal" (A2A solo en Google ADK y CrewAI).
- **8 sandboxes oficiales** + UnixLocal built-in; **3 session stores** + SQLite built-in + OpenAI Conversations API server-managed.
- **Realtime Python solo server-side WebSocket** (NO WebRTC; para browser usar SDK JS/TS sibling `openai/openai-agents-js`).
- **Voice pipeline 3-step** (STT → code → TTS) via `VoicePipeline` distinto de Realtime Agents (que es bidireccional).
- **Tracing OpenTelemetry-like** jerárquico Trace → Task → Turn → Agent → spans especializados; ZDR excluye tracing.
- **Comparativa cruzada con LangGraph (37k★), AutoGen (60k★), Google ADK (~21k★), CrewAI (55k★)** — verificada via shields.io para los 4, contrastada con datos de JWIKI-014 y JWIKI-012.
- **Patrón handoffs como借鉴 directo para Aithera V1.0 Orchestrator** (handoff como tool callable; tool name `transfer_to_<agent_name>`; input_type BaseModel para metadata; on_handoff callback; nest_handoff_history; input_filter).

Doc final: **5148 palabras**, 23 secciones TEMPLATE.md, 18 snippets con path:line, 35 URLs en sección Fuentes, tabla comparativa 5 frameworks × 17 criterios, 88% confianza. 6/6 criterios CONSTITUTION.md §8 cumplidos (código revisado, fuentes contrastadas — GitHub API rate-limited pero compensado con raw/shields/docs, compatibilidad documentada — 8 sandboxes + 3 session stores + 2 routers, ejemplos verificados — 8 ejemplos copy-paste ready, referencias cruzadas — JWIKI-002/007/009/010/011/012/013/014, revisión independiente — contraste contra 4 frameworks via shields.io live).

## Próximo

**JWIKI-016** (siguiente ID pending de menor número según wiki-map.md tras verificar task_queue).

## Pendientes cross-doc (NO hechos en este tick — regla 1 tick = 1 doc)

- [ ] Re-leer `JWIKI-002 projects.md` y actualizar fila OpenAI Agents SDK: ~27.7k → 28k stars, agregar v0.18.0, MIT (no es cambio, ya estaba), Python >=3.10 (fuente: este tick).
- [ ] Re-leer `JWIKI-010 agent-frameworks.md` y actualizar fila OpenAI Agents SDK: v0.18.0, 28k stars, sandbox agents (new in 0.14.0), realtime con gpt-realtime-2.1 (fuente: este tick).
- [ ] Re-leer `JWIKI-014 google-adk.md` y agregar contraste Realtime OpenAI vs Gemini Live (realtime agents son first-class en OpenAI SDK; Gemini Live es lo paralelo en ADK).
- [ ] Re-leer `JWIKI-012 crewai.md` y agregar contraste sesiones: OpenAIConversationsSession (server-managed, ref-only) vs CrewAI Unified Memory (client-side, multi-scope) — ambos válidos para producción.
- [ ] Re-leer `JWIKI-013 autogen.md` y refutar/refinar claim sobre A2A como "transversal a todos los frameworks" — NO es transversal; OpenAI Agents SDK NO tiene A2A nativo, solo Google ADK y CrewAI.
- [ ] Re-leer `JWIKI-011 langgraph.md` y agregar comparativa patrón grafo vs handoffs: LangGraph conditional_edges (graph-based) vs OpenAI handoffs (tool-based). Ambos válidos; elección depende del modelo mental.
- Razón: regla 1 tick = 1 doc (P3); el próximo tick que toque cada doc debe absorber estos fixes.