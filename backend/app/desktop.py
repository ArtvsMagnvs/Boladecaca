# ELIMINADO — Auditoría v0.9.5, hallazgo #9 (deuda CLAUDE.md §16.8, graphify 2026-07-15).
#
# Este archivo contenía la app CustomTkinter legacy (AitheraApp, 1207 líneas,
# 11 `except: pass`), sustituida por Electron desde PLAN_HUB_VISUAL_Y_VOZ.md.
# Cero imports entrantes en todo el backend (verificado por grep en la auditoría).
#
# Recuperable con: git show v0.9.5 -- backend/app/desktop.py
#
# El archivo no se borra físicamente en esta sesión (el entorno de trabajo no
# permite borrados); eliminarlo del todo con:  git rm backend/app/desktop.py
