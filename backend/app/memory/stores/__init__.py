# app/memory/stores/ — implementaciones internas de IMemoryStore/ISkillStore.
#
# INTERNO del paquete app.memory (doc 16 §4.1). El resto de la app NO importa
# de aqui directamente: habla contra `memory_router` / `skill_store` exportados
# por app.memory. Las fronteras las vigila tests/test_module_boundaries.py.
